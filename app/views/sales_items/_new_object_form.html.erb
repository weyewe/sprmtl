<% if new_object.persisted? %>
	<% id = 'edit_object'%>
<% else %>
	<% id = 'new_object'%>
<% end %>

<%= simple_form_for( new_object, :url => url,
      :method => :post , 
      :html => {:class => " form-horizontal prevent_double_submit ajax-form   no_content_form",
        :id => "#{id}"}) do |f| %>

	 
<!-- 				new_object.material_id        = params[:material_id]       
		    new_object.is_pre_production  = params[:is_pre_production] 
		    new_object.is_production      = params[:is_production]     
		    new_object.is_post_production = params[:is_post_production]
		    new_object.is_delivered       = params[:is_delivered]      
		    new_object.delivery_address   = params[:delivery_address]  
		    new_object.price_per_piece    =  BigDecimal( params[:price_per_piece ])
		    new_object.weight_per_piece    =  BigDecimal( params[:weight_per_piece ])
		    new_object.quantity           = params[:quantity]          
		    new_object.description        = params[:description]   
		    new_object.delivery_address   = params[:delivery_address]          
		    new_object.requested_deadline = params[:requested_deadline]
		
		 -->

   
 

	<%= f.input :material_id, :label => "Material" do %>
     <%= f.select :material_id, 
         Material.all_selectables %>
  <% end %>

	<hr />
		<%= f.input :name, :label =>"Nama Item", :hint => "Nama item menurut customer"   %>
		<%= f.input :description, :label =>"Deskripsi item",
		 			:input_html => {:class => "input-xlarge ", :rows => '5'} ,
		 			:hint => "Deskripsi tambahan mengenai item tersebut" %>
	
	
	<hr />
		<%= f.input :quantity, :label =>"Kuantitas"   %>
		<%= f.input :weight_per_piece, :label =>"Berat satuan", 
				:hint => "Dalam kilogram, sensitif sampai 2 decimal. Contoh: <b>2.15</b>kg"   %>
		<%= f.input :price_per_piece, :label =>"Harga satuan",
		   	:hint => "Dalam rupiah. Langsung isi tanpa koma. <br />"+ 
							"Contoh untuk isi sembilan puluh lima ribu:"  + 
							"<b>95000</b>" 		%>

	<hr />
		<%= f.input :is_pre_production, :label =>"Pattern?", :hint => "Termasuk jasa pembuatan pattern?" %>
		<%= f.input :pre_production_price, :label =>"Biaya Pola",
		   	:hint => "Dalam rupiah. Langsung isi tanpa koma. <br />"+ 
							"Contoh untuk isi sembilan puluh lima ribu:"  + 
							"<b>95000</b>" 		%>
							
		<br />
		<%= f.input :is_production, :label =>"Casting?", :hint => "Termasuk jasa casting?"  %>
		<%= f.input :production_price, :label =>"Biaya Cetak",
		   	:hint => "Dalam rupiah. Langsung isi tanpa koma. <br />"+ 
							"Contoh untuk isi sembilan puluh lima ribu:"  + 
							"<b>95000</b>" 		%>
		<%= f.input :is_pricing_by_weight, :label =>"Biaya Cetak Berdasarkan Berat?", 
			:hint => "Centang jika biaya berdasarkan berat. Jika berdasarkan quantity, jangan di centang"  %>					
		<br />
		
		<%= f.input :is_post_production, :label =>"Machining?", :hint => "Termasuk jasa bubut?"  %>
		<%= f.input :post_production_price, :label =>"Biaya Bubut",
		   	:hint => "Dalam rupiah. Langsung isi tanpa koma. <br />"+ 
							"Contoh untuk isi sembilan puluh lima ribu:"  + 
							"<b>95000</b>" 		%>
	
	<hr />
		<%= f.input :is_delivered, :label =>"Delivery", :hint => "Centang jika diantar"  %>
		<%= f.input :delivery_address, :label =>"Alamat Delivery" ,
		  	:input_html => {:class => "input-xlarge ", :rows => '5'}%>
	
		
	
	

  <div class="form-actions">
	
		<% if new_object.persisted? %>
			<%= f.button :submit , :class => "btn btn-primary", :value=> "Update Sales Item" %>
		<% else %>
			<%= f.button :submit , :class => "btn btn-primary", :value=> "Create Sales Item" %>
		<% end %>
		
  </div>
<% end %>


 